name: "Tests"

on: [pull_request]
jobs:
  tests:
    name: Unit & E2E
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform: [amd64, arm64, arm/v7, ppc64le, windows/amd64]

    env:
      PLATFORM: ${{ matrix.platform == 'arm/v7' && 'armv7' || matrix.platform == 'windows/amd64' && 'windows' || matrix.platform }}

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 2
        submodules: recursive

    - run: git checkout HEAD^2
    
    - name: Install dependencies
      run: composer install --ignore-platform-reqs --no-interaction --prefer-dist


    - name: Set up QEMU
      uses: docker/setup-qemu-action@v3
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Build image for ${{ matrix.platform }}
      uses: docker/build-push-action@v3
      with:
        context: .
        push: false
        tags: database-dev:${{ matrix.platform == 'arm/v7' && 'armv7' || matrix.platform == 'windows/amd64' && 'windows' || matrix.platform }}
        load: true
        cache-from: type=gha
        cache-to: type=gha,mode=max
        platforms: ${{ matrix.platform == 'windows/amd64' && matrix.platform || format('linux/{0}', matrix.platform) }}

    - name: Start Containers
      run:  |
        docker compose up -d
        sleep 10

    - name: Run Tests
      run: |
        if [[ "${{ matrix.platform }}" == "amd64" ]]; then
            docker compose exec tests vendor/bin/phpunit --configuration phpunit.xml --testsuite X86;
        elif [[ "${{ matrix.platform }}" == "arm64" ]]; then
            docker compose exec tests vendor/bin/phpunit --configuration phpunit.xml --testsuite ARM64;
        elif [[ "${{ matrix.platform }}" == "arm/v7" ]]; then
            docker compose exec tests vendor/bin/phpunit --configuration phpunit.xml --testsuite ARMV7;
        elif [[ "${{ matrix.platform }}" == "ppc64le" ]]; then
            docker compose exec tests vendor/bin/phpunit --configuration phpunit.xml --testsuite PPC;
        elif [[ "${{ matrix.platform }}" == "windows/amd64" ]]; then
            docker compose exec tests vendor/bin/phpunit --configuration phpunit.xml --testsuite Windows;
        fi